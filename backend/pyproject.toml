[project]
name = "holdview-backend"
version = "0.1.0"
description = "Backend API for Holdview"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.136.3",
    "uvicorn[standard]>=0.48.0",
]
